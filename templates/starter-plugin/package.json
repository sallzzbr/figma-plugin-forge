{
  "name": "starter-plugin",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.104.0",
    "esbuild": "^0.24.0",
    "tailwindcss": "^3.4.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "preact": "^10.25.0"
  }
}
